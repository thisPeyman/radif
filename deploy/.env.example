# Temporary disposable-data testing only. Replace all three values for HTTPS.
SITE_ADDRESS=http://SERVER_IP
APP_ORIGIN=http://SERVER_IP
COOKIE_SECURE=false

# Generate with: openssl rand -hex 32
POSTGRES_PASSWORD=replace-with-a-random-hex-value
POSTGRES_BIND=127.0.0.1:5432

# Use: id -u && id -g
APP_UID=1000
APP_GID=1000

SESSION_LIFETIME=720h
MAX_RECEIPT_BYTES=5242880
